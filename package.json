{
  "name": "vuetsfullstack",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \" cd proj.server && npm start \" \" cd PROJ.CLIENT && npm start \"",
    "dev": "concurrently \" cd proj.server && npm run dev \" \" cd PROJ.CLIENT && npm run dev \"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
